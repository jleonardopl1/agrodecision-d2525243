# AgroDecision · Design System

Identidade visual do produto, consolidada em código. Página viva em `/design-system`.

| Camada | Arquivo |
| --- | --- |
| Valores de marca | `src/lib/design-tokens.ts` |
| Tema (CSS vars) | `src/index.css` |
| Utilitários | `tailwind.config.ts` |

Cores: `campo` #1A5C38 · `campo-claro` #E8F5EC · `colheita` #F59E0B · `sinal-atencao` #DC2626.
Sinais: VENDER (campo) · AGUARDAR (colheita) · ATENCAO (vermelho) — componente `SinalBadge`.
Tipografia: Inter. Raio base: 12px.
